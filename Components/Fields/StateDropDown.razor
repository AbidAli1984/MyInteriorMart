<label>State <span class="text-danger">*</span></label>
<select class="form-control" @onchange="GetCitiesByStateId">
    <option value="0" selected>Choose...</option>
    @if (ListWizAddressVM.States != null)
    {
        @foreach (var p in ListWizAddressVM.States)
        {
            @if (ListWizAddressVM.StateId == p.StateID)
            {
                <option selected value="@p.StateID">@p.Name</option>
                continue;
            }
            <option value="@p.StateID">@p.Name</option>
        }
    }
</select>